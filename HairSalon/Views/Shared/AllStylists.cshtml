@using HairSalonApp.Models;
<!DOCTYPE html>
@{
  Layout = "_Layout";
}

  <h2>All Stylists:</h2>
      @if (Model != null)
      {
        foreach(Stylist stylist in Stylist.GetAll())
        {
          <a href="/stylists/details/@stylist.GetId()"><h4>@stylist.GetName()</h4></a>
          <hr>
        }
      }

      <a href="/stylists/new">Add a new Stylist</a>
    </div>
